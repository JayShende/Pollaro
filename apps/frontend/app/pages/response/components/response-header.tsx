import React from 'react'

interface ResponseHeaderProps {
    totalResponses: number;
}
const ResponseHeader = ({ totalResponses }: ResponseHeaderProps) => {
  return (
    <div>ResponseHeader</div>
  )
}

export default ResponseHeader